// PLACEHOLDER: LBR-69 will replace this with the real calls service.
// Keeping the empty class export so the module wiring compiles in M1.
import { Injectable } from '@nestjs/common';

@Injectable()
export class CallsService {}
